using UnityEngine;
using UnityEngine.InputSystem;

public class Player_movement: MonoBehaviour
{
    /* public Vector2 inputDir;
    public void OnMove(InputAction.CallbackContent context) => inputDir = context.ReadValue<Vector2>();

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        // transform.position = new Vector3(0f, 5f, 4.5f);   
    }

    // Update is called once per frame
    void Update()
    {
        transform.position += newVector3(0f, 00015f, 0f);
        // consolenBefehle
        // Debug.Log("");
    }
}
    */
}
